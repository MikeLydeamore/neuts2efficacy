name: Build Targets
on: [push]
jobs:
  Build-Action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls ${{github.workspace}}
      - run: Rscript ./setup_packages.R
      - run: Rscript -e "library(targets); tar_make()"